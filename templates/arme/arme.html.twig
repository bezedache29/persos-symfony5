{% extends 'base.html.twig' %}

{% block title %}Armes{% endblock %}

{% block monTitre %}Arme {{ arme.nom }}{% endblock %}

{% block body %}
    <div class="row align-items-center">
        <div class="col-2">
            <img src="{{ asset('images/armes/' ~ arme.image ~ '.png') }}" alt="">
        </div>
        <div class="col">
            <p class="m-0">{{ arme.nom }}</p>
            <p class="m-0">{{ arme.description }}</p>
            <p class="m-0">Dégats : {{ arme.degats }}</p>
        </div>
    </div>
{% endblock %}
